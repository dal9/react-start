export default function reducer(state, action) {
    switch (action.type) {
        case 'list':
            return {

            };
        case 'post' :
            return {

            };
        case 'edit' :
            return {

            };
        default:
            throw new Error();
    }
}
